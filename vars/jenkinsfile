@Library("shared") _
pipeline {
    agent { label "vinod" }
    stages {
        stage("hello from groovy") {
            steps {
                echo "hello from agent"
                hellow()   // This calls the function from your shared library
                echo "hello ji"
            }
        }
    }
}
