pipeline{
    agent {label "vinod"}
    stages{
        stage("checking") {
            steps{
                echo "i am from agent side"
                sh 'hostname'
            }
        }
    }
}
